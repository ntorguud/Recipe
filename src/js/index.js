/// MVC -Model, View, Controller Archictecture
import query from "./model/search";
import {add} from "./view/searchView";

console.log(`Hailt ${query}`);
console.log(`Amount of two numbers is ${add(4, 6)}`);
